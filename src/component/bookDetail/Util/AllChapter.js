import {View, Text} from 'react-native';
import React from 'react';

const AllChapter = ({navigation, route}) => {
  return (
    <View>
      <Text>AllChapter</Text>
    </View>
  );
};

export default AllChapter;
